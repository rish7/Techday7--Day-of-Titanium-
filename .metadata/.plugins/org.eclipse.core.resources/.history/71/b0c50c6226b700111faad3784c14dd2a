var win=Ti.UI.createWindow({backgroundColor:"#FFF"});


// Create a Label.
var lab = Ti.UI.createLabel({
	text : 'My Label with my styles',	
	className:'myClass'	
});


var btn=Ti.UI.createButton({
	Title:'My button in my style'
	});

// Add to the parent view.
win.add(lab);


win.open();
